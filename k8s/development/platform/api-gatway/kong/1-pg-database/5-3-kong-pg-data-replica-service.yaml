apiVersion: v1
kind: Service
metadata:
  name: kong-pg-data-replica-svc
  namespace: kong-data
spec:
  type: ClusterIP
  selector:
    app: kong-pg-data-replica
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432

# DNS kong-pg-data-replica-svc.kong-data.svc.cluster.local